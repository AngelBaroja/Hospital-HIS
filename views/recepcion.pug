doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title HIS Hospital - Recepción
    link(rel="stylesheet" href="/estilos.css")
  
  body 
    .contenedor 
      .contenido-pestaña.activa#recepcion
        h2 Datos Básicos del Paciente
        p Complete la información inicial para comenzar el proceso de admisión.

        form#formulario-recepcion
          .formulario-grid
            .grupo-formulario
              label(for="tipo-paciente") Tipo de Paciente 
              select#tipo-paciente(required)
                option(value="") Seleccione...
                option(value="nuevo") Nuevo paciente
                option(value="existente") Paciente existente
                option(value="emergencia") Emergencia
            
            .grupo-formulario
              label(for="id-paciente") ID/Cédula 
              input#id-paciente(type="text" placeholder="Número de identificación" required)
          
          .botones-accion
            button.boton.boton-secundario(type="button" disabled) Atrás
            button.boton.boton-primario(type="button" onclick="siguientePestana('registro')") Continuar
    
    script(src="/script.js")